{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run saas:check:prod && npm run build",
  "framework": "vite",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
